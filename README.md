# How to install
1. run `python -m venv .venv`
2. in `.venv/scripts` run the activate script for your OS
3. you should now have (.venv) on your cli terminal
4. run pip install argon2-cffi
5. repeat steps 2-3 whenever you start a new terminal session
6. run `python main.py`